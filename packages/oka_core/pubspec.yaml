name: oka_core
description: Core contracts for oka — typed build context, composable pipeline (BuildStep, Artifact, Pipeline), and the declarative Oka composition root (ADR-0006).
version: 0.1.6
homepage: https://github.com/Arenukvern/oka
repository: https://github.com/Arenukvern/oka

environment:
  sdk: ^3.12.0

dependencies:
  args: ^2.5.0
  from_json_to_json: ^0.5.0
  meta: ^1.14.0
  path: ^1.9.0
  yaml: ^3.1.2

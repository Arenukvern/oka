name: oka
description: AI-powered Flutter Android build system that replaces Gradle for faster builds
version: 0.1.1
homepage: https://github.com/yourusername/oka

environment:
  sdk: ^3.5.0

dependencies:
  args: ^2.5.0
  path: ^1.9.0
  yaml: ^3.1.2
  process: ^5.0.2
  watcher: ^1.1.0
  from_json_to_json: ^0.2.0
  http: ^1.2.1
  crypto: ^3.0.3
  archive: ^3.4.10
  io: ^1.0.4
  cli_util: ^0.4.1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0

executables:
  oka:
